{
  "name": "gemprice-frontend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bunx --bun vite dev --host 0.0.0.0 --port 3000",
    "build": "bunx --bun vite build",
    "preview": "bunx --bun vite preview",
    "check": "bunx --bun svelte-kit sync && bunx svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^2.0.0",
    "@sveltejs/kit": "^1.20.4",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "svelte": "^4.0.5",
    "tailwindcss": "^3.3.6",
    "typescript": "^5.0.0",
    "vite": "^4.4.2"
  },
  "type": "module",
  "dependencies": {
    "@auth0/auth0-spa-js": "^2.3.0",
    "@tailwindcss/forms": "^0.5.7"
  }
}
